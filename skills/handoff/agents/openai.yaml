interface:
  display_name: "Handoff"
  short_description: "Create concise Markdown handoff packages, optionally with a code zip"
  default_prompt: "Use $handoff to package the current work into a concise Markdown handoff with summary, status, next steps, evidence, and relevant context, adding a code zip when the receiver cannot reach the repo."
